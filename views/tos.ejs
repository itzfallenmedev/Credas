<!DOCTYPE html>
<html lang="en">
<head>
<%- include('partials/credas-head', { pageTitle: settings.tosPageTitle, pageDesc: settings.tosPageSubtitle }) %>
</head>
<body>
<%- include('partials/navbar', { user, existingUser, currentPath: '' }) %>

<main id="main">
<section class="page-head">
  <div class="wrap">
    <p class="crumb"><a href="/">Home</a><span>/</span>Terms of service</p>
    <h1 class="t-h1"><%= settings.tosPageTitle %></h1>
    <p class="t-lead" style="margin-top:var(--sp-3)"><%= settings.tosPageSubtitle %></p>
  </div>
</section>

<section style="padding-bottom:var(--sp-8)">
  <div class="wrap">
    <div class="prose"><%- md.render(settings.termsOfService) %></div>
  </div>
</section>
</main>

<%- include('partials/footer') %>
</body>
</html>
