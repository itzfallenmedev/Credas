<!DOCTYPE html>
<html lang="en">
<head>
<%- include('partials/credas-head', { pageTitle: settings.privacyPolicyPageTitle, pageDesc: settings.privacyPolicyPageSubtitle }) %>
</head>
<body>
<%- include('partials/navbar', { user, existingUser, currentPath: '' }) %>

<main id="main">
<section class="page-head">
  <div class="wrap">
    <p class="crumb"><a href="/">Home</a><span>/</span>Privacy policy</p>
    <h1 class="t-h1"><%= settings.privacyPolicyPageTitle %></h1>
    <p class="t-lead" style="margin-top:var(--sp-3)"><%= settings.privacyPolicyPageSubtitle %></p>
  </div>
</section>

<section style="padding-bottom:var(--sp-8)">
  <div class="wrap">
    <div class="prose"><%- md.render(settings.privacyPolicy) %></div>
  </div>
</section>
</main>

<%- include('partials/footer') %>
</body>
</html>
